upstream minecraft-build {
	hash     	$remote_addr consistent;

	server   	{{ minecraft.ip_address }}:{{ minecraft.port }};
}

server {
	listen   	0.0.0.0:{{ minecraft.bind_port }} reuseport;

	proxy_connect_timeout  	1s;
	proxy_timeout     	3s;
	proxy_pass   	minecraft-{{ minecraft.name }};
}

